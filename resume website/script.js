var _a;
// Print function to trigger the browser's print dialog
function printResume() {
    window.print();
}
// Add an event listener to the print button
(_a = document.getElementById("print-btn")) === null || _a === void 0 ? void 0 : _a.addEventListener("click", printResume);
